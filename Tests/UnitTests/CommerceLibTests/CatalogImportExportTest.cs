// The following code was generated by Microsoft Visual Studio 2005.
// The test owner should check each test for validity.
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Text;
using System.Collections.Generic;
using Mediachase.Commerce.Catalog.ImportExport;
using System.IO;
namespace UnitTests
{
    /// <summary>
    ///This is a test class for Mediachase.Commerce.ImportExport.CatalogImportExport and is intended
    ///to contain all Mediachase.Commerce.ImportExport.CatalogImportExport Unit Tests
    ///</summary>
    [TestClass()]
    public class CatalogImportExportTest
    {


        private TestContext testContextInstance;

        /// <summary>
        /// Gets or sets the test context which provides
        /// information about and functionality for the current test run.
        /// </summary>
        /// <value>The test context.</value>
        public TestContext TestContext
        {
            get
            {
                return testContextInstance;
            }
            set
            {
                testContextInstance = value;
            }
        }
        #region Additional test attributes
        // 
        //You can use the following additional attributes as you write your tests:
        //
        //Use ClassInitialize to run code before running the first test in the class
        //
        //[ClassInitialize()]
        //public static void MyClassInitialize(TestContext testContext)
        //{
        //}
        //
        //Use ClassCleanup to run code after all tests in a class have run
        //
        //[ClassCleanup()]
        //public static void MyClassCleanup()
        //{
        //}
        //
        //Use TestInitialize to run code before running each test
        //
        //[TestInitialize()]
        //public void MyTestInitialize()
        //{
        //}
        //
        //Use TestCleanup to run code after each test has run
        //
        //[TestCleanup()]
        //public void MyTestCleanup()
        //{
        //}
        //
        #endregion


        /// <summary>
        /// A test for Export (string, Stream)
        /// </summary>
        [TestMethod()]
        public void ExportTest()
        {
            string catalogName = "Mediachase Catalog";

            Stream output = new FileStream("C:\\test.exp", FileMode.Create);

            new CatalogImportExport().Export(catalogName, output, "path");

            output.Flush();
            output.Close();

            ExportTestHelp();

            Assert.Inconclusive("A method that does not return a value cannot be verified.");
        }

        private void ExportTestHelp()
        {
            Stream input = new FileStream("C:\\test.exp", FileMode.Open);
            try
            {
                new CatalogImportExport().Import(input, Guid.NewGuid(), "ImportExport");
            }
            finally
            {
                input.Close();
            }
        }


        /// <summary>
        /// A test for Import (Stream)
        /// </summary>
        [TestMethod()]
        public void ImportTest()
        {
            

            Stream input = new FileStream("C:\\test.exp", FileMode.Open);

            try
            {
                new CatalogImportExport().Import(input, Guid.NewGuid(), "ImportExport");
            }
            finally
            {
                input.Close();
            }

            Assert.Inconclusive("A method that does not return a value cannot be verified.");
        }
    }


}
